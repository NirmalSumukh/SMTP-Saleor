# The key used for metadata encryption. Required for production builds
# Generate a secure key with: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-min-32-chars-abc123

# APL Configuration (Application Persistence Layer)
# https://docs.saleor.io/developer/extending/apps/developing-apps/app-sdk/apl
# Options: file, rest, upstash, saleor-cloud
APL=file
# Path for file-based APL (when APL=file)
FILE_APL_PATH=data/.apl-data.json

# REST APL (when APL=rest)
# REST_APL_ENDPOINT=
# REST_APL_TOKEN=

APP_LOG_LEVEL=debug

# Variable controlling Saleor domains that app can be installed on.
# It's a regex pattern.
ALLOWED_DOMAIN_PATTERN=.*

# Local development variables
# APP_IFRAME_BASE_URL=http://localhost:3010/smtp
# APP_API_BASE_URL=http://host.docker.internal:3010/smtp

# DynamoDB Configuration (required for production)
# For development, you can use a local DynamoDB or skip this feature
DYNAMODB_MAIN_TABLE_NAME=smtp-app-dev
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=placeholder-access-key
AWS_SECRET_ACCESS_KEY=placeholder-secret-key
DYNAMODB_REQUEST_TIMEOUT_MS=5000
DYNAMODB_CONNECTION_TIMEOUT_MS=3000

# OTEL related variables
OTEL_ENABLED=false

# Environment
ENV=local
NODE_ENV=development

# Fallback SMTP configuration (optional)
# Uncomment and fill ALL values to enable fallback email sending
# FALLBACK_SMTP_HOST=smtp.example.com
# FALLBACK_SMTP_PORT=587
# FALLBACK_SMTP_USER=your-smtp-user
# FALLBACK_SMTP_PASSWORD=your-smtp-password
# FALLBACK_SMTP_ENCRYPTION=TLS
# FALLBACK_SMTP_SENDER_NAME=My App
# FALLBACK_SMTP_SENDER_EMAIL=noreply@example.com
